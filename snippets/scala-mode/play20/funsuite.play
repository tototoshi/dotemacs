# name: funsuite.play
# key: fun
# --
package $1

import play.api.test._
import play.api.test.Helpers._
import org.scalatest.{ FunSuite, BeforeAndAfter, OptionValues }
import org.scalatest.matchers.ShouldMatchers

class ${2:`(substring (first (reverse (split-string (buffer-file-name) "/"))) 0 -6) `} extends FunSuite with BeforeAndAfter with ShouldMatchers with OptionValues {

before {

}

after {

}

test("$0") {
}

}

