# name: funsuite
# key: fun
# --
import org.scalatest.{ FunSuite, BeforeAndAfter, OptionValues }
import org.scalatest.matchers.ShouldMatchers

class ${1:`(substring (first (reverse (split-string (buffer-file-name) "/"))) 0 -6) `} extends FunSuite with BeforeAndAfter with ShouldMatchers with OptionValues {

before {

}

after {

}

}

