#name: spec
# --
import org.scalatest.FunSpec
import org.scalatest.matchers.ShouldMatchers

class ${1:`(substring (first (reverse (split-string (buffer-file-name) "/"))) 0 -6) `} extends FunSpec with ShouldMatchers {
  def fixture = new {

  }

  describe("$1") {

    it("$0") {

    }

  }
}
